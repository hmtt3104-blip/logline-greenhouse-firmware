# Reference

This clean export was sanitized from active working firmware for the Waveshare ESP32-S3 dual-zone greenhouse controller.

Private configuration, real credentials, live network details, deployment topology, logs, backups, binaries, and old repository history are excluded.

Use this directory only for public-safe reference notes. Do not place private source dumps here.
